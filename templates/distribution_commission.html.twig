{#
/**
 * @file distribution_commission.html.twig
 * Default theme implementation to present Commission data.
 *
 * This template is used when viewing Commission pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_distribution_commission()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('distribution_commission') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
